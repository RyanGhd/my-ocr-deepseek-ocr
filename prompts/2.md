i need the following packages to be downloaded and added to the notebook

```
torch==2.6.0
transformers==4.46.3
tokenizers==0.20.3
einops
addict 
easydict
pip install flash-attn==2.7.3 --no-build-isolation
```

create the content of the requirement file for me so that I can copy paste it inside the file

python -m pip download -r requirements.txt -d wheelhouse_linux_py310  --platform manylinux2014_x86_64 --python-version 310 --implementation cp --abi cp310  --only-binary=:all: