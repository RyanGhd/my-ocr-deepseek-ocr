# goal 
build a docker file that has some packages installed on the image

# steps
1. use `us-docker.pkg.dev/deeplearning-platform-release/gcr.io/workbench-container:latest` as the base image
2. add the command to install the following packages:
```
torch==2.6.0
transformers==4.46.3
tokenizers==0.20.3
einops
addict 
easydict
```

3. convert the following command to a docker command that installs the package:
```
pip install flash-attn==2.7.3 --no-build-isolation
```
s
